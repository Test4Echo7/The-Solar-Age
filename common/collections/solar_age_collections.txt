non_capitulated_equus_countries = {
    input = game:all_countries
    name = COLLECTION_NON_CAPITULATED_EQUUS_COUNTRIES
    operators = {
        limit = {
            NOT = { tag = EQS }
            capital_scope = {
                is_on_continent = europe
            }
            exists = yes
            has_capitulated = no
        }
    }
}

subjugated_equus_countries = {
    input = collection:non_capitulated_equus_countries
    name = COLLECTION_SUBJUGATED_EQUUS_COUNTRIES
    operators = {
        limit = {
            is_subject_of = EQS
        }
    }
}

griffonian_pony_countries = {
    input = game:all_countries
    name = COLLECTION_GRIFFONIAN_PONY_COUNTRIES
    operators = {
        limit = {
            capital_scope = {
                is_on_continent = asia
            }
            has_idea = pony_race
        }
    }
}

zebrican_pony_and_adjacent_countries = {
    input = game:all_countries
    name = COLLECTION_ZEBRICAN_PONY_AND_ADJACENT_COUNTRIES
    operators = {
        limit = {
            capital_scope = {
                is_on_continent = africa
            }
            OR = {
                has_idea = pony_race
                has_idea = kirin_race
                has_idea = horse_race
            }
        }
    }
}

non_capitulated_pony_and_adjacent_countries = {
    input = game:all_countries
    name = COLLECTION_NON_CAPITULATED_PONY_AND_ADJACENT_COUNTRIES
    operators = {
        limit = {
            NOT = { tag = EQS }
            exists = yes
            has_capitulated = no
            OR = {
                has_idea = pony_race
                has_idea = kirin_race
                has_idea = horse_race
            }
        }
    }
}

subjugated_pony_and_adjacent_countries = {
    input = collection:non_capitulated_pony_and_adjacent_countries
    name = COLLECTION_SUBJUGATED_PONY_AND_ADJACENT_COUNTRIES
    operators = {
        limit = {
            is_subject_of = EQS
        }
    }
}

unoccupied_GRI_territories = {
    input = collection:former_imperial_territories
    name = COLLECTION_UNOCCUPIED_GRI_TERRITORIES
    operators = {
        limit = {
            OWNER = {
                NOT = {
                    OR = {
                        tag = EQS
                        AND = {
                            tag = GRD
                            has_country_leader = { character = GRI_daybreaker ruling_only = yes }
                        }
                    }
                }
            }
        }
    }
}

griffonian_trained_griffon_countries = {
    input = game:all_countries
    name = COLLECTION_GRIFFONIAN_TRAINED_GRIFFON_COUNTRIES
    operators = {
        limit = {
            is_griffon_continent_griffon_country = yes
            is_in_faction_with = ROOT
            has_mastery = {
                amount = 600
                track = infantry
            }
        }
    }
}
